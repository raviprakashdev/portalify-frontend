import { portalifyConstants } from './portalify-constants';
describe('portalifyConstants', () => {
  it('should work', () => {
    expect(portalifyConstants()).toEqual('portalify-constants');
  });
});
