import { portalifyDataAccess } from './portalify-data-access';
describe('portalifyDataAccess', () => {
  it('should work', () => {
    expect(portalifyDataAccess()).toEqual('portalify-data-access');
  });
});
